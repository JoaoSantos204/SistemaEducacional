@model IEnumerable<Academico.Models.Disciplina>

@{
    ViewData["Title"] = "Index";
}

<h1>Principal</h1>

<p>
    <a asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CargaHoraria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Curso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CargaHoraria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curso.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DisciplinaID">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.DisciplinaID">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.DisciplinaID">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
